node_modules
.env
coverage
coverage.json


#Hardhat files
cache
#typechain need the types to be built in dapp by vercel, after building the types are erased in build anyways
#artifacts same
artifacts/build-info