node_modules
.env
coverage
coverage.json
cache

# Keep generated types to refer to in dapp frontend
# Needed for building in CI, even though types are erased afterwards
# typechain 

# Keep artifacts/contracts, as the abi is used in dapp frontend
artifacts/build-info
artifacts/hardhat
